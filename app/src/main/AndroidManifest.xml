<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.workout_tracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_omino_pesi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_omino_pesi_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Workout_tracker">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".WorkoutActivity">

        </activity>
        <activity android:name=".LoginActivity">

        </activity>
        <activity android:name=".TransitionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".Services.Timer">

        </service>
    </application>

</manifest>